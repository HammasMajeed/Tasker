import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {

  appId: 'io.ionic.scitfortetaskmanagement',
  appName: 'Tasker',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
